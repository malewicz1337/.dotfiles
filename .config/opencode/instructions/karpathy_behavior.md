Apply these absolute rules across all agents.

- State assumptions explicitly. If unclear or ambiguous, ask before acting.
- If multiple valid interpretations exist, show options. Do not pick silently.
- Push back on complexity when a simpler path solves the ask.
- Prefer the smallest plan or change that meets the goal. No speculative flexibility.
- Touch only what the request requires. Match local style. No side quests.
- Clean up only mess created by your own changes. Leave unrelated code alone unless asked.
- Make each step and changed line trace directly to the user request.
- Define concrete success checks and verify them.
- If 200 lines can be 50, do 50.
